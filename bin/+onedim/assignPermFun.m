function [erVec] = assignPermFun(mesh,epr)
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% assignPermFun
%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%
% creates a permittivity vector erVec based on the information in the mesh
% object "mesh" and the function handle "epr"
%
% inputs
%   mesh        [struct]        generated by onedim.generateMesh
%   epr         [function]      relative permittivity function
%
% outputs
%   erVec       [N x 1 double]  array of relative permittivities at mesh.z
%
% example: 
% >> mesh = onedim.generateMesh(N,h);
% >> epr = @(z) z*e0;
% >> mesh.epr = mesh.epr = onedim.assignPermFun(mesh,epr); 
%
% kurt schab -- kschab@scu.edu
% scu 
% 2021

    z = mesh.z;
    erVec = epr(z);
end